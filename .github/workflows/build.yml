name: Build

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "20"

      - name: Install dependencies
        run: npm install
      
      - name: Install ESLint and Prettier
        run: npm install eslint@9 @eslint/js globals typescript-eslint prettier

      - name: Lint with ESLint
        run: npm run lint

      - name: Build
        run: npm run build
